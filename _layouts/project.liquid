---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  <article>
    {{ content }}
  </article>

  {% if page.collabarators %}
    <h2>Collaborators</h2>
    <div>
      <ul>
      {% for collabarator in page.collabarators %}
        <li>{{ collabarator }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  {% if page.collabarators %}
    <h2>Funding</h2>
    <div>
      <ul>
      {% for funder in page.funding %}
        <li>{{ collabarator }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

</div>
